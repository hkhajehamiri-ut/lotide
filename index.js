// index.js
const assertArraysEqual   = require('./assertArraysEqual');      // might need to remove them later
const assertEqual = require('./assertEqual');                    // might need to remove them later
const assertObjectEqual  = require('./assertObjectEqual');    // might need to remove them later
const countLetters   = require('./countLetters');
const countOnly   = require('./countOnly');
const eqArrays   = require('./eqArrays');     // might need to remove them later
const eqObjects   = require('./eqObjects');    // might need to remove them later
const findKey   = require('./findKey');
const findKeyByValue   = require('./findKeyByValue');
const flatten   = require('./flatten');
const head   = require('./head');
const letterPositions   = require('./letterPositions');
const map   = require('./map');
const middle = require('./middle');
const tail   = require('./tail');
const takeUntil   = require('./takeUntil');
const without   = require('./without');

module.exports = {
  assertArraysEqual,   // might need to remove them later
  assertEqual,               // might need to remove them later
  assertObjectEqual,   // might need to remove them later
  countLetters,
  countOnly,
  eqArrays,   // might need to remove them later
  eqObjects,  // might need to remove them later
  findKey,
  findKeyByValue,
  flatten,
  head,
  letterPositions,
  map,
  middle,
  tail,
  takeUntil,
  without

};